spring:
  mvc:
    throw-exception-if-no-handler-found: true
  profiles:
    active: dev

server:
  port: 8081
  servlet:
    context-path: /common-services/v1
management:
  server:
    port: 9060
  endpoint:
    web:
      base-path: /actuator

logging:
  file:
    path: /comman-services/logs
